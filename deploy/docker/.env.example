# ============================================================================
# DocSynth Self-Hosted Deployment Configuration
# Copy this to .env and fill in your values
# ============================================================================

# ──────────────────────────────────────────────────────────────────────────
# Required
# ──────────────────────────────────────────────────────────────────────────
JWT_SECRET=your-jwt-secret-at-least-32-characters-long

# ──────────────────────────────────────────────────────────────────────────
# LLM Provider (choose one)
# ──────────────────────────────────────────────────────────────────────────

# Option A: Anthropic Claude (cloud)
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-...

# Option B: OpenAI (cloud)
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-...

# Option C: Ollama (self-hosted, air-gapped)
# LLM_PROVIDER=ollama
# OLLAMA_BASE_URL=http://ollama:11434
# OLLAMA_MODEL=llama3.1:70b

# Option D: Azure OpenAI (enterprise)
# LLM_PROVIDER=azure-openai
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
# AZURE_OPENAI_API_KEY=your-key
# AZURE_OPENAI_DEPLOYMENT=your-deployment

# Option E: AWS Bedrock
# LLM_PROVIDER=aws-bedrock
# AWS_BEDROCK_REGION=us-east-1

# ──────────────────────────────────────────────────────────────────────────
# SCM Provider
# ──────────────────────────────────────────────────────────────────────────

# GitHub (or GitHub Enterprise Server)
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# For GitHub Enterprise Server, uncomment:
# GITHUB_BASE_URL=https://github.yourcompany.com/api/v3

# GitLab (or GitLab Self-Managed)
# GITLAB_URL=https://gitlab.com
# GITLAB_TOKEN=

# Bitbucket (or Bitbucket Data Center)
# BITBUCKET_URL=https://api.bitbucket.org/2.0
# BITBUCKET_TOKEN=

# ──────────────────────────────────────────────────────────────────────────
# Database & Cache (defaults work with bundled services)
# ──────────────────────────────────────────────────────────────────────────
# DATABASE_URL=postgresql://docsynth:docsynth@postgres:5432/docsynth
# REDIS_URL=redis://redis:6379
# POSTGRES_USER=docsynth
# POSTGRES_PASSWORD=docsynth
# POSTGRES_DB=docsynth

# ──────────────────────────────────────────────────────────────────────────
# Ports (defaults)
# ──────────────────────────────────────────────────────────────────────────
# API_PORT=3001
# POSTGRES_PORT=5432
# REDIS_PORT=6379

# ──────────────────────────────────────────────────────────────────────────
# Application URL
# ──────────────────────────────────────────────────────────────────────────
# APP_URL=http://localhost:3000
